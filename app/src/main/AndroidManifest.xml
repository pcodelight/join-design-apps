<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pcodelight.joindesign">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".BaseApp"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".screen.MaterialDetailScreen"/>
        <activity android:name=".screen.DashboardScreen" />
        <activity android:name=".screen.StoreSelectionScreen" android:theme="@style/AppTheme.ActionBar"
            android:label="@string/choose_store"/>
        <activity android:name=".screen.LoginScreen" android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>